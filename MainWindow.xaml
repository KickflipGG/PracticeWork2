<Window x:Class="PracticeWork2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticeWork2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#FF76E383">
            <TextBlock Text="Практическая работа №2" VerticalAlignment="Center" 
                       Margin="20,0,0,0" FontSize="18" FontWeight="Bold" Foreground="#1A3320"/>
            <Button x:Name="btnBack" HorizontalAlignment="Right" Content="Назад в меню" 
                    Width="120" Height="36" Margin="0,0,20,0" FontSize="14" Cursor="Hand"
                    Click="btnBack_click"/>
        </Grid>

        <Frame x:Name="FrmMain" Grid.Row="1" NavigationUIVisibility="Hidden" 
               ContentRendered="FrmMain_ContentRendered"/>

    </Grid>
</Window>
